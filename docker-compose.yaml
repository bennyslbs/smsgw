version: '3.3'

services:
  smsgw:
    container_name: smsgw
    build:
      context: .
      dockerfile: ./container/Dockerfile
    restart: always
    devices:
      - "/dev/gsmmodemMySim:/dev/gsmmodemMySim"
    ports:
      - '2525:2525'
